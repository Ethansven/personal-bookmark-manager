# Frontend dev .env — SPA only needs the API base.
# All Auth0 configuration lives in backend/.env (see backend/.env.example).
# The backend owns the OIDC round-trip; the SPA stores the resulting token
# in localStorage and uses it as a Bearer credential.
VITE_API_BASE_URL=http://localhost:3000